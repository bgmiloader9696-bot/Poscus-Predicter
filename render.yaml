services:
  - type: web
    name: poscus-predicter
    env: python
    region: oregon
    plan: free
    buildCommand: pip install --upgrade pip && pip install -r requirements.txt
    startCommand: python start.py
    healthCheckPath: /
